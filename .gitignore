node_modules/
out/
release/
dist/
server/dist/

# 运行时数据（账号号池、配置、SSO 存档）
out/accounts.json
out/sso/
data/
./data/
docker/data/
register/config.json
register/logs/
register/sso/
register/__pycache__/
register/**/*.pyc

# sing-box 二进制：镜像构建时下载，勿提交（体积大，见 docker/Dockerfile）
register/bin/sing-box/linux-amd64
register/bin/sing-box/linux-arm64
register/bin/sing-box/linux-arm

# 环境变量
.env
docker/.env

# editor / OS junk
.vscode/
.idea/
.fleet/
.DS_Store
Thumbs.db
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

uploads/


# runtime build stamp (docker/CI writes this)
register/BUILD_ID
BUILD_ID

# 本地文档 / 交接 / 计划 — 不推远端（仅本机）
docs/
.superpowers/

# 一次性补丁脚本、本地备份、手输 OTP 等 — 不推远端
register/_patch_*.py
register/_config_bak*
register/*_bak*.json
register/_manual_otp.txt
register/_*.tmp
register/_*.bak

# local test cache
.pytest_cache/

# 本地测试目录（测试用 config 备份、测试产出的 SSO 等）— 不推远端
register/_localtest/

